<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dhguissepe.platzi.conf">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlatziConf">
        <activity android:name="com.dhguissepe.platzi.conf.view.ui.activities.SplashScreenActivity"></activity>
        <!-- Toda pantalla de nuestra aplicación es considerada una activity. Y se registra aquí. -->
        <activity android:name=".view.ui.activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>